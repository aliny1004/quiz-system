{
  "name": "pqs",
  "version": "13.3.0",
  "private": true,
  "description": "Practice Quiz System static frontend for Vercel.",
  "scripts": {
    "build": "node scripts/generate-env.js",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
